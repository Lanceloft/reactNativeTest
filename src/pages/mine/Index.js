import React from 'react';
import {
  Button, View, Text,
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({

})


export default class HomeScreen extends React.Component {
  render() {
    return (
      <View>
        <View style={styles.configHeader}></View>
      </View>
    );
  }
}
